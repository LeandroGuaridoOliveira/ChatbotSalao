<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Novo agendamento</title>
<style>
body { font-family: Arial; background: #ede7f6;}
.central { margin: 60px auto; max-width: 410px; background: #fff; border-radius: 8px; box-shadow: 0 2px 8px #ccc; padding: 32px;}
input,select { width:100%;padding:8px;margin-bottom:11px;border-radius:4px;border:1px solid #ccc;}
button { background: #7e57c2; color: #fff; border:none; width:100%; padding:8px 0; font-weight:bold; border-radius:4px;}
a { color: #7e57c2;}
h3 { color: #7e57c2;}
</style>
</head>
<body>
<div class="central">
<h3>Novo Agendamento</h3>
<form method="post">
    <input name="nome" placeholder="Nome completo do cliente" required>
    <input name="cpf" placeholder="CPF do cliente" required>
    <input name="dia" type="date" required>
    <input name="horario" placeholder="Ex: 14:00" required>
    <select name="procedimento_id" required>
    <option value="">Escolha o procedimento...</option>
    <% procedimentos.forEach(p=>{ %>
        <option value="<%= p.id %>"><%= p.nome %> (R$ <%= p.valor %>)</option>
    <% }) %>
    </select>
    <button type="submit">Gravar</button>
</form>
<div style="font-size:14px;"><a href="/agendamentos">&larr; Voltar à lista</a></div>
</div>
</body>
</html>