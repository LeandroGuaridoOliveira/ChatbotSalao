<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Login — Salão</title>
    <style>
        body { font-family: Arial; background: #ede7f6; }
        .central { margin: 110px auto; width: 320px; background: #fff; border-radius: 8px; box-shadow: 0 2px 5px #ccc; padding: 32px;}
        input[type="text"],input[type="password"] { width: 100%; padding: 8px; margin-bottom: 16px; border: 1px solid #ccc;}
        button { background: #7e57c2; color: #fff; border: none; width: 100%; padding: 9px 0; font-weight: bold; border-radius: 4px;}
        .erro { color: #d32f2f; margin-bottom: 8px;}
        h2 { color: #7e57c2; text-align:center;}
    </style>
</head>
<body>
<div class="central">
    <h2>Salão — Login</h2>
    <% if (erro) { %>
        <div class="erro"><%= erro %></div>
    <% } %>
    <form method="POST" action="/login">
        <input type="text" name="login" placeholder="Usuário" required>
        <input type="password" name="senha" placeholder="Senha" required>
        <button type="submit">Entrar</button>
    </form>
    <div style="color:#777; font-size:12px; margin-top:18px;">Dica inicial: admin / admin123</div>
</div>
</body>
</html>